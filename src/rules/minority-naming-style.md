# `minority-naming-style`

Reports a function, type, or constant whose casing differs from the project's dominant casing for that declaration kind.

**Why it matters.** A project that deliberately differs from the official guide still benefits from being internally predictable.

**When it matters.** A convention requires at least 20 declarations and a 90% majority. Foreign and generated declarations are excluded.
